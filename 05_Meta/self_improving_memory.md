{"content":"---\ntags: [meta, self-improving, learning, hot]\n---\n# Memory (HOT Tier) 🦞\n\n_Last updated: 2026-04-27_\n_Compiled from first full day of operation + enhanced pipeline build_\n\n## Preferences\n- Shawn prefers sharp, analytical, zero-fluff communication\n- Use DeepSeek v4-flash for conversation; DeepSeek Reasoner for complex analysis\n- HF free models ONLY for prediction pipeline (dual-API enforcement)\n- GitHub push protection scans for secrets — never hardcode tokens in scripts\n- n8n workflows must use proper UUID node IDs (not string aliases) or import fails\n- WriteFile node type is not available in n8n 2.x — use noOp instead\n- Dashboard should show: target date, reasoning breakdown, service health, control buttons\n- Always log corrections and patterns to HOT memory immediately\n- **Dashboard must show REAL data only** — never mock, fake, or simulated\n\n## Network & Services\n- **HF Router fix:** Use `https://router.huggingface.co/hf-inference/models/` not `api-inference.huggingface.co/models/`\n- **Gateway health:** GET http://127.0.0.1:18789/health → `{\"ok\":true,\"status\":\"live\"}`\n- **n8n is NOT auth-free for REST API** — CLI must be used for all programmatic workflow management\n- **Dashboard proxy (5500):** Specific endpoints (gateway-health, n8n-health, trigger-n8n, log) must be above generic `/api/*` catchall\n\n## Technical Patterns\n- **yfinance MultiIndex:** Single ticker downloads return MultiIndex columns — use `.xs('Close', axis=1, level=0).squeeze()`\n- **n8n import format:** Requires wrapping array `[...]`, UUIDs on all node IDs, `typeVersion` on every node, no `writeFile`, tags must be empty `[]`\n- **Prediction scoring:** Weighted combo (sentiment 35% + returns 30% + RSI 20% + volume 15%), normalized with min/max caps\n- **Yahoo scraping:** Change% embedded in price column cell — use regex `\\((\\+?\\d+\\.?\\d*)%\\)` to extract\n- **n8n Python generators:** More reliable than hand-writing JSON for 20+ node workflows — use uuid.uuid4() for all IDs\n- **n8n webhook:** Production URL (`POST /webhook/<path>`) only works after workflow toggle on/off in UI\n- **Services die after session ends** — use `Start-Process -WindowStyle Hidden` for persistence\n\n## Pipeline Architecture (Enhanced v2 - 29 nodes)\n- **CONFIG node** at top of pipeline with all weights, URLs, thresholds as configurable parameters\n- **RAG sentiment:** HF call → memory retrieve → blend (70% current + 30% historical if >3 entries)\n- **Memory:** n8n Static Data stores last 20 predictions/ticker + 50 sentiment entries/ticker\n- **DynamoDB:** `stock_historical_data` table, partition key=ticker, sort key=date\n- **Error handling:** Switch/IF nodes on Universe and Prices with dedicated error fallbacks\n- **Batch processing:** Split Into Batches node (configurable 50/batch) for large universes\n- **Universe override:** Webhook payload can set `universe` parameter, falls back to CONFIG value\n\n## Rules\n- Before any git push: verify no API keys/tokens are in the staged files\n- Unicode chars (─, 🦞, ✅) cause cp1252 errors — use `-X utf8` flag on Python or ASCII replacements\n- HOT memory must be populated with actionable patterns, not left empty\n- Dashboard API calls must go through proxy chain (5500 → 18888/5678/18789) — never direct\n- Prediction pipeline must use live yfinance + real HF Inference API — no local data generation\n- **Capital preservation first — never lose principal**\n"}