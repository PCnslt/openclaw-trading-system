---
tags: [memory, daily]
---
# 2026-04-27 Daily Memory 🦞

## Late Night / Early Morning (Apr 26->27)

### What Happened
- Finalized n8n multi-agent workflow import — fixed UUID node IDs, regenerated proper JSON, successfully imported
- Created prediction dashboard at http://localhost:5500 showing live prediction output
- Prediction server (18888) returns reasoning breakdown (per-stock RSI, volume, sentiment, score, return_5d, return_1d)
- Dashboard includes: Run Prediction button, Trigger n8n button, service health status, target date badge
- Self-improving HOT memory populated with 12 patterns/rules from the first day of operation
- Corrections log updated with 4 learnings
- Git pushed: `87d6210` (dashboard + memory updates)

### Lessons Learned
- n8n requires proper UUID node IDs — string aliases cause import to silently produce empty files
- Services die after session ends; must use `Start-Process -WindowStyle Hidden` for persistence
- Dashboard server proxies work best as a unified proxy (5500) that forwards to prediction server (18888) and n8n (5678)
- n8n webhooks work WITHOUT API keys — use `POST /webhook/run-pipeline` for trigger control
- Prediction server should return `target_date`, `reasoning` array, and `base_date` for a complete dashboard
- Dashboard should show per-stock scoring breakdown with "why" column (momentum, sentiment, volume signals)

### System State at End of Session
- Gateway (18789): running
- Dashboard (7000): running
- n8n (5678): running, workflow imported & ready
- Prediction Server (18888): running, returns target_date + reasoning data
- Prediction Dashboard (5500): running, proxied API

### Next Steps
- Import the multi-agent workflow into n8n if not yet done
- Generate n8n API key via UI for programmatic workflow control
- Run prediction pipeline against full 300+ stock universe
- Build evaluation dashboard showing hit rate over time
